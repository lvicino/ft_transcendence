
services:
  auth-api:
    build: 
      target: development
    volumes:
      - ./auth-api:/usr/src/app
      - /usr/src/app/node_modules
    ports:
      - "3001:3000"

  game-api:
    build:
      target: development
    volumes:
      - ./game-api:/usr/src/app
      - /usr/src/app/node_modules
    ports:
      - "3002:3000"